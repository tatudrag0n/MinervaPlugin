name: Strengthen Warrior kit

on:
  push:
    paths:
      - .github/scripts/strengthen_warrior.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  strengthen-warrior:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Warrior buff
        run: python3 .github/scripts/strengthen_warrior.py
      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - name: Build
        run: mvn -B -DskipTests package
      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo "No source changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/org/server/minerva/FfaConfig.java src/main/java/org/server/minerva/FfaKit.java
          git commit -m "Strengthen Warrior melee damage"
          git push
