name: Strengthen Bug Mania probabilities

on:
  push:
    paths:
      - .github/scripts/strengthen_bug_mania_probabilities.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  strengthen-bug-mania:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Bug Mania probability tuning
        run: python3 .github/scripts/strengthen_bug_mania_probabilities.py
      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - name: Build
        run: mvn -B -DskipTests package
      - name: Commit source changes
        run: |
          if git diff --quiet; then
            echo "No source changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main/java/org/server/minerva/FfaManager.java src/main/java/org/server/minerva/FfaConfig.java
          git commit -m "Strengthen Bug Mania probability effects"
          git push
